version: "2"
linters:
  default: none
  enable:
    - errcheck
    - govet
    - revive
    - unused
    - staticcheck
  settings:
    revive:
      rules:
        - name: exported
          disabled: true
    staticcheck:
      checks:
        - all
        - -QF* # style refactor suggestions
        - -ST1* # doc comment style rules
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
      # Terminal writes and cleanup close calls where errors are not actionable
      - linters:
          - errcheck
        source: "(fmt\\.Fprint|fmt\\.Fprintf|resp\\.Body\\.Close|term\\.Restore)"
formatters:
  enable:
    - gofmt
